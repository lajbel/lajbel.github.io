scripts:
  build:
    desc: "Build src/ folder to .html static"
    cmd: lume --src src/ --dest page/
  serve: 
    desc: "Server site"
    cmd: lume --serve --src src/ 
  dev:
    desc: All for developement
    cmd: vr build && vr serve